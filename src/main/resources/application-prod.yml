spring:
  datasource:
    url: jdbc:mysql://localhost:3306/srdjan_prod
    username: sfg_prod_user
    password: srx
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false